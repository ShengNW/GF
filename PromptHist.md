其实单一入口、不依赖session,靠md同步这个模式是非常具有创新性的，如果能够实现就可以吧codex当成纯粹算力，因为模型回答也是需要时间的，有时候它思索时我会看前面的回答，然后有新的问题我可能直接会再开一个codex接着去问。所以请帮我在女友目录创建PromptHist.md，并在.gitignore放行，然后在AGENTS.md中加上“规则：所有在项目根目录开的codex的用户prompt都必须记录在项目根目录的PromptHist.md文件中，而且这个文件只记录用户prompt。”这个，弄完推git（注意我以上说的都是女友目录、GF.git）
